# mail
简单邮件服务器



# 快捷安装
```
cd /www/server/mdserver-web/plugins && rm -rf mail && git clone https://github.com/mw-plugin/mail && cd mail && rm -rf .git && cd /www/server/mdserver-web/plugins/mail && bash install.sh install 1.0
```