# mail
简单邮件服务器



# 快捷安装
```
cd /www/server/mdserver-web/plugins && rm -rf imail && git clone https://github.com/mw-plugin/imail && cd imail && rm -rf .git && cd /www/server/mdserver-web/plugins/imail && bash install.sh install 1.0
```